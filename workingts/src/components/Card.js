
// you show list
// you sort them
// you highlight type of sort 

const Card = ({ id, name, year, type="none" }) => {
  return (
    <idv>
      <div>{id}, {name}, {year}</div>
      <div>{id},{name}, {year}</div>
      <div>{id}, {name}, {year}</div>
    </idv>
  );
};

export default Card
